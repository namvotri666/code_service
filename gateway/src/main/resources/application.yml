server:
  port: 8081

spring:
  cloud:
    gateway:
      routes:
        - id: user-service
          uri: http://localhost:5000
          predicates:
            - Path=/auth/login

        - id: ticket-service
          uri: http://localhost:5001
          predicates:
            - Path=/ticket/get
